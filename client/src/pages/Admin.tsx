import { trpc } from "@/lib/trpc";
import { Activity, Database, LockKeyhole, RadioTower, ShieldAlert } from "lucide-react";

export default function Admin() { const { data } = trpc.platform.adminOverview.useQuery(); return <div className="space-y-8"><section><p className="text-xs font-bold uppercase tracking-[0.18em] text-violet-200">Restricted workspace</p><h2 className="mt-2 text-3xl font-semibold tracking-tight text-white sm:text-4xl">Admin dashboard</h2><p className="mt-3 max-w-2xl text-slate-400">Operational visibility for users, ads, rewards, campaigns, orders, and security events. Sensitive actions remain role-gated on the server.</p></section><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[{ label: "Verified rewards", value: data?.rewards ?? 0, icon: Activity }, { label: "Reward sessions", value: data?.sessions ?? 0, icon: RadioTower }, { label: "Ledger entries", value: "Server", icon: Database }, { label: "Security posture", value: "Active", icon: LockKeyhole }].map(item => { const Icon = item.icon; return <div key={item.label} className="rounded-3xl border border-white/[0.07] bg-white/[0.035] p-5"><Icon className="text-violet-200" size={19} /><p className="mt-5 text-sm text-slate-500">{item.label}</p><p className="mt-1 text-2xl font-semibold text-white">{item.value}</p></div>; })}</div><div className="grid gap-6 lg:grid-cols-2"><div className="rounded-3xl border border-white/[0.07] bg-white/[0.035] p-6"><div className="flex items-center gap-3"><ShieldAlert className="text-emerald-200" size={19} /><h3 className="font-semibold text-white">Security controls</h3></div><ul className="mt-5 space-y-3 text-sm text-slate-400"><li>• Webhook HMAC verification with timestamp freshness</li><li>• Unique provider transaction IDs and replay protection</li><li>• Server-owned points ledger and role authorization</li><li>• Rate limiting for reward session creation</li><li>• Immutable audit events for reward actions</li></ul></div><div className="rounded-3xl border border-white/[0.07] bg-white/[0.035] p-6"><h3 className="font-semibold text-white">Launch checklist</h3><div className="mt-5 space-y-3 text-sm text-slate-400"><p>Connect a real rewarded ad provider adapter.</p><p>Store provider credentials in the deployment secret manager.</p><p>Confirm local advertising, privacy, and contest requirements.</p><p>Keep payments disabled until a verified provider flow is approved.</p></div></div></div></div>; }
