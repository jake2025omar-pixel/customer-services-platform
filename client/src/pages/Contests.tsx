import { trpc } from "@/lib/trpc";
import { CalendarDays, ChevronRight, ShieldCheck, Users } from "lucide-react";

export default function Contests() {
  const { data, isLoading } = trpc.contests.list.useQuery();
  return <div className="space-y-8"><section><p className="text-xs font-bold uppercase tracking-[0.18em] text-emerald-200">Fair participation</p><h2 className="mt-2 text-3xl font-semibold tracking-tight text-white sm:text-4xl">Contests & rewards</h2><p className="mt-3 max-w-2xl text-slate-400">Every campaign is published with eligibility, official rules, dates, and a transparent winner selection method. Watching an ad or buying points never guarantees a win.</p></section><div className="grid gap-5 xl:grid-cols-2">{isLoading ? <div className="text-slate-400">Loading campaigns...</div> : data?.map(contest => <article key={contest.id} className="rounded-[28px] border border-white/[0.07] bg-white/[0.035] p-6 transition hover:border-emerald-300/20 hover:bg-white/[0.05]"><div className="flex items-start justify-between gap-4"><div><span className="inline-flex rounded-full bg-violet-300/10 px-3 py-1.5 text-xs font-bold text-violet-200">Active campaign</span><h3 className="mt-4 text-2xl font-semibold tracking-tight text-white">{contest.name}</h3></div><div className="grid h-11 w-11 shrink-0 place-items-center rounded-2xl bg-emerald-300/10 text-emerald-200"><ChevronRight size={20} /></div></div><div className="mt-6 rounded-2xl bg-[#0d1a2a] p-4"><p className="text-xs uppercase tracking-[0.14em] text-slate-500">Prize</p><p className="mt-2 font-semibold text-white">{contest.prize}</p></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div className="rounded-2xl border border-white/[0.06] p-3"><p className="flex items-center gap-2 text-xs text-slate-500"><CalendarDays size={14} /> Dates</p><p className="mt-2 text-slate-200">{new Date(contest.startsAt).toLocaleDateString()} – {new Date(contest.endsAt).toLocaleDateString()}</p></div><div className="rounded-2xl border border-white/[0.06] p-3"><p className="flex items-center gap-2 text-xs text-slate-500"><Users size={14} /> Participants</p><p className="mt-2 text-slate-200">{contest.participants} entries · {contest.tickets} tickets</p></div></div><div className="mt-5 space-y-3 text-sm leading-6 text-slate-400"><p><span className="font-semibold text-slate-200">Eligibility:</span> {contest.eligibility}</p><p><span className="font-semibold text-slate-200">Official rules:</span> {contest.rules}</p><p><span className="font-semibold text-slate-200">Winner selection:</span> {contest.winnerSelection}</p><p><span className="font-semibold text-slate-200">Prize delivery:</span> {contest.prizeDelivery}</p></div><div className="mt-6 flex items-center gap-2 border-t border-white/[0.07] pt-5 text-xs text-emerald-200"><ShieldCheck size={15} /> Complete legal information is required before a campaign can be published.</div></article>)}</div></div>;
}
